const RAW_NOTES = `
Devletin unsurları: Bağımsızlık (Oksızlık), ülke, halk (Kün) ve teşkilat
Türk devletlerinde hükümdarın eşine verilen unvan: Hatun (Katun)
Gerektiğinde devlet başkanlığı (Naiblik) yapabilen kişi: Hatun
Kurultay hatun kararlarının ilan şekli: “Hakan veya Hatun buyurur ki…”
Hükümdarın çadırı: Otağ
Hükümdarlık davulu: Nevbet
Sancak, bağımsızlık sembolü: Tuğ
Taht: Örgin
Ziyafet, şölen: Toy
Sorguç: Kotuz
Hükümdarlık alameti olan kemer: Kur (Kemer)
Kamçı: Berge
Bıçak: Kama
Dünyanın doğudan batıya Türk hükümdarları tarafından yönetilmesi düşüncesi: Türk Cihan Hâkimiyeti
Dünyada düzen ve adalet sağlama anlayışı: Nizam-ı Âlem
Ulaşılan her hedeften sonra yeni hedef belirleme ülküsü: Kızılelma
Devleti yönetme yetkisi (siyasi güç): Kut
Askerî güç ve kuvvet: Küç
Ekonomik pay, hisse, nasip: Ülüş
Gök Tanrı’nın hükümdara devleti yönetme yetkisi verdiğine inanılması: Kut anlayışı
Devletin doğu kanadını yöneten hükümdar: Kağan
Devletin batı kanadını yöneten yönetici: Yabgu
Devletin merkezi kabul edilen bölüm: Doğu
İkili teşkilatı ilk uygulayan Türk devleti: Asya Hun Devleti
İkili teşkilatın olumsuz sonucu: Taht kavgalarına yol açması
Hükümet: Ayuki
Vezir: Aygucı (Üge)
Bakan: Buyruk
Vergi memuru: Tudun
Sivil yönetici: Apa
Hazineden sorumlu görevli: Ağılıg
Hekim, tabip: Otacı (Emçi)
Askerî vali: Tutuk
Oğuzlarda hükümdar: Erkin
Yazıcı ve mühürdar: Bitikçi (Tamgaçı)
Hakanın emri: Yarlığ
Hükümdar oğlu: Tigin
Tigin eğitmeni: İnanç (İnal veya Ataman)
Hanedan üyesi komutan: Şad
Günümüz Bakanlar Kurulunun karşılığı: Kurultay
Kağan olmadığında kurultaya başkanlık eden kişi: Aygucı
Kurultaya katılan üyelere verilen ad: Toygun
Kurultaydaki oturma düzeninin adı: Orun
Devlet ve toplum meselelerinin görüşüldüğü meclis: Kurultay
Töreye uymayan hakanı görevden alabilen ve yeni hakan seçebilen kurum: Kurultay
Sadece danışma değil, karar alma görevi de bulunan meclis: Kurultay
Hazar Devleti’nin meclisi: İhtiyarlar Meclisi
Peçeneklerin meclisi: Komenton
Aile: Oguş
Sülale: Urug
Boy: Boy
Millet: Budun
Devlet: İl
Sosyal düzeni sağlayan örf, âdet, gelenek ve ahlaki değerlere dayanan sözlü hukuk kuralları: Töre
Mahkeme: Yargu
Mahkeme başkanı (yargıç): Yargan (Yargucu)
Devlet ile vatandaşın karşılıklı görevlerini belirleyen yazısız sözleşme: Tüz
Törede adalet ilkesi: Könilik
Törede eşitlik ilkesi: Tüzlük
Törede iyilik ilkesi: Uzluk
Törede insanlık ilkesi: Kişilik
Kara Kuvvetleri Komutanlığının kuruluş yılı olarak kabul edilen tarih: Mete Han’ın tahta çıkışı (MÖ 209)
Keşif birliği: Yelme
Orduda asker: Sü / Er
Ordu komutanı: Tarkan
Türklerin kullandığı ilk takvim: 12 Hayvanlı Türk Takvimi
Türklerin tarih boyunca kullandığı takvimler: 12 Hayvanlı Türk Takvimi, Hicri Takvim, Celâli Takvimi, Rumi Takvim, Miladi Takvim
Dünyanın en eski halısı: Pazırık Halısı
Altın Elbiseli Adam: Asya Hunlarına ait önemli madencilik eseri
Balbal taşlarının yontulmasıyla gelişen sanat dalı: Heykel
Türklerin tarih boyunca inandığı dinler: Gök Tanrı, Manihaizm, İslamiyet, Musevilik, Budizm, Hristiyanlık
Atalar Kültü: Mezar ziyareti ve atalara saygı anlayışı
Yuğ: Cenaze töreni
Kurgan: Mezar
Balbal (Bengütaş): Mezar taşı
Tamu: Cehennem
Uçmağ: Cennet
Kült: İnanç
Parayı kullanan ilk Türk devleti: Göktürkler
Millî alfabeler: Göktürk, Uygur
Sav: Atasözü
Koşuk: Şiir
Sagu: Ağıt
Hikâye: Dede Korkut Hikâyeleri
Destan: Kahramanlık hikâyeleri
Alp Er Tunga Destanı: İskitler
Oğuz Kağan Destanı: Asya Hunları (Oğuz Kağan = Mete Han)
Ergenekon ve Bozkurt Destanları: Göktürkler
Türeyiş ve Göç Destanları: Uygurlar
Attila ve Sihirli Geyik Destanı: Avrupa Hunları
Manas Destanı: Kırgızlar
Türk edebiyatının bilinen ilk yazılı örnekleri: Yenisey Kitabeleri
Türk edebiyat tarihinin bilinen ilk yazılı eseri: Orhun Abideleri
Türk tarihinin ilk siyasetnamesi: Orhun Abideleri
Orhun Abideleri’nin bulunduğu yer: Moğolistan
Orhun Abideleri’nin yazıldığı alfabeler: Orhun (Göktürk) ve Çin alfabesi
Orhun Abideleri’nde kullanılan yazı türü: Runik yazı
Orhun’daki ilk kitabe: Tonyukuk Kitabesi (725)
Orhun’daki ikinci kitabe: Kültiğin Kitabesi (732)
Orhun’daki üçüncü kitabe: Bilge Kağan Kitabesi (735)
Orhun yazısını çözen kişi: W. Thomsen
İlk Türk tarihçisi ve yazarı: Vezir Tonyukuk
`;

const TOPICS = [
  { id: "all", name: "Tüm konular", from: 1, to: 102 },
  { id: "devlet", name: "Devlet ve hükümdarlık", from: 1, to: 25 },
  { id: "yonetim", name: "Yönetim ve kurultay", from: 26, to: 48 },
  { id: "toplum-hukuk", name: "Toplum ve hukuk", from: 49, to: 61 },
  { id: "ordu-sanat", name: "Ordu, takvim ve sanat", from: 62, to: 70 },
  { id: "inanc", name: "İnanç ve kültür", from: 71, to: 80 },
  { id: "edebiyat", name: "Edebiyat ve destanlar", from: 81, to: 91 },
  { id: "yazitlar", name: "Yazıtlar ve Orhun", from: 92, to: 102 },
];

function normalizeAnswer(value) {
  return value.trim().replace(/[.]+$/, "").toLocaleLowerCase("tr-TR");
}

function getAnswerKey(value) {
  return normalizeAnswer(value).replace(/\s*\([^)]*\)\s*/g, " ").trim();
}

function getTopicForId(id) {
  return TOPICS.find((topic) => topic.id !== "all" && id >= topic.from && id <= topic.to);
}

const QUESTIONS = RAW_NOTES.trim().split("\n").map((line, index) => {
  const separatorIndex = line.indexOf(":");
  const id = index + 1;

  return {
    id,
    prompt: line.slice(0, separatorIndex).trim(),
    answer: line.slice(separatorIndex + 1).trim().replace(/[.]+$/, ""),
    topic: getTopicForId(id)?.id ?? "all",
  };
});

const state = {
  session: [],
  index: 0,
  solved: 0,
  errors: 0,
  candidateDeck: [],
  selectedCandidate: null,
  completedQuestionIds: new Set(),
  mistakes: [],
  selectedTopic: TOPICS[0],
};

const els = {
  setupPanel: document.querySelector("#setupPanel"),
  quizPanel: document.querySelector("#quizPanel"),
  resultPanel: document.querySelector("#resultPanel"),
  topicSelect: document.querySelector("#topicSelect"),
  questionCountSelect: document.querySelector("#questionCountSelect"),
  startButton: document.querySelector("#startButton"),
  restartButton: document.querySelector("#restartButton"),
  correctCount: document.querySelector("#correctCount"),
  answeredCount: document.querySelector("#answeredCount"),
  errorCount: document.querySelector("#errorCount"),
  topicBadge: document.querySelector("#topicBadge"),
  questionCounter: document.querySelector("#questionCounter"),
  definitionCard: document.querySelector("#definitionCard"),
  questionText: document.querySelector("#questionText"),
  answerGrid: document.querySelector("#answerGrid"),
  matchButton: document.querySelector("#matchButton"),
  feedbackPanel: document.querySelector("#feedbackPanel"),
  feedbackTitle: document.querySelector("#feedbackTitle"),
  feedbackText: document.querySelector("#feedbackText"),
  resultTitle: document.querySelector("#resultTitle"),
  resultText: document.querySelector("#resultText"),
  retryMistakesButton: document.querySelector("#retryMistakesButton"),
  newSessionButton: document.querySelector("#newSessionButton"),
};

function shuffle(items) {
  const copy = [...items];
  for (let index = copy.length - 1; index > 0; index -= 1) {
    const randomIndex = Math.floor(Math.random() * (index + 1));
    [copy[index], copy[randomIndex]] = [copy[randomIndex], copy[index]];
  }
  return copy;
}

function renderTopicOptions() {
  els.topicSelect.innerHTML = TOPICS.map((topic) => {
    const count = QUESTIONS.filter((question) => topic.id === "all" || question.topic === topic.id).length;
    return `<option value="${topic.id}">${topic.name} (${count})</option>`;
  }).join("");
}

function getQuestionPool(topicId) {
  return topicId === "all" ? QUESTIONS : QUESTIONS.filter((question) => question.topic === topicId);
}

function startSession(customQuestions = null) {
  state.selectedTopic = TOPICS.find((topic) => topic.id === els.topicSelect.value) ?? TOPICS[0];

  if (customQuestions) {
    state.session = shuffle(customQuestions);
    state.selectedTopic = { id: "mistakes", name: "Zorlandığım kartlar" };
  } else {
    const pool = shuffle(getQuestionPool(state.selectedTopic.id));
    const requestedCount = els.questionCountSelect.value === "all"
      ? pool.length
      : Number(els.questionCountSelect.value);
    state.session = pool.slice(0, Math.min(requestedCount, pool.length));
  }

  state.index = 0;
  state.solved = 0;
  state.errors = 0;
  state.candidateDeck = [];
  state.selectedCandidate = null;
  state.completedQuestionIds = new Set();
  state.mistakes = [];

  els.setupPanel.hidden = true;
  els.resultPanel.hidden = true;
  els.quizPanel.hidden = false;
  updateScore();
  renderQuestion();
}

function buildCandidateDeck(question) {
  const blockedAnswers = new Set(
    QUESTIONS
      .filter((item) => item.prompt === question.prompt)
      .map((item) => getAnswerKey(item.answer))
  );

  const uniqueCandidates = [];
  const seen = new Set(blockedAnswers);
  const candidateGroups = [
    QUESTIONS.filter((item) => (
      item.id !== question.id
      && !state.completedQuestionIds.has(item.id)
      && item.topic === question.topic
      && !blockedAnswers.has(getAnswerKey(item.answer))
    )),
    QUESTIONS.filter((item) => (
      item.id !== question.id
      && !state.completedQuestionIds.has(item.id)
      && item.topic !== question.topic
      && !blockedAnswers.has(getAnswerKey(item.answer))
    )),
    QUESTIONS.filter((item) => (
      item.id !== question.id
      && state.completedQuestionIds.has(item.id)
      && !blockedAnswers.has(getAnswerKey(item.answer))
    )),
  ];

  candidateGroups.flatMap((group) => shuffle(group)).forEach((item) => {
    const answerKey = getAnswerKey(item.answer);
    if (!seen.has(answerKey)) {
      seen.add(answerKey);
      uniqueCandidates.push(item.answer);
    }
  });

  return shuffle([question.answer, ...uniqueCandidates.slice(0, 9)]);
}

function renderQuestion() {
  const question = state.session[state.index];
  state.candidateDeck = buildCandidateDeck(question);
  state.selectedCandidate = null;

  els.topicBadge.textContent = state.selectedTopic.name;
  els.questionCounter.textContent = `Kart çifti ${state.index + 1} / ${state.session.length}`;
  els.questionText.textContent = question.prompt;
  renderCandidateCards();
}

function renderCandidateCards() {
  els.answerGrid.innerHTML = "";
  state.candidateDeck.forEach((candidate) => {
    const button = document.createElement("button");
    button.className = "answer-card";
    button.type = "button";
    button.dataset.answer = candidate;
    button.textContent = candidate;
    button.setAttribute("aria-pressed", "false");
    button.addEventListener("click", () => selectCandidateCard(button));
    els.answerGrid.append(button);
  });

  els.definitionCard.classList.remove("is-wrong");
  els.feedbackPanel.hidden = true;
  els.feedbackPanel.classList.remove("is-wrong");
  els.matchButton.disabled = true;
}

function selectCandidateCard(selectedButton) {
  els.answerGrid.querySelectorAll(".answer-card").forEach((button) => {
    button.classList.remove("is-selected", "is-wrong");
    button.setAttribute("aria-pressed", "false");
  });

  state.selectedCandidate = selectedButton.dataset.answer;
  selectedButton.classList.add("is-selected");
  selectedButton.setAttribute("aria-pressed", "true");
  els.definitionCard.classList.remove("is-wrong");
  els.feedbackPanel.hidden = true;
  els.feedbackPanel.classList.remove("is-wrong");
  els.matchButton.disabled = false;
}

function updateScore() {
  els.correctCount.textContent = String(state.solved);
  els.answeredCount.textContent = String(state.session.length || 0);
  els.errorCount.textContent = String(state.errors);
}

function rememberMistake(question) {
  if (!state.mistakes.some((item) => item.id === question.id)) {
    state.mistakes.push(question);
  }
}

function showDecisionError(question) {
  const selectedButton = els.answerGrid.querySelector(".answer-card.is-selected");
  state.errors += 1;
  updateScore();
  rememberMistake(question);
  selectedButton?.classList.add("is-wrong");
  selectedButton?.classList.remove("is-selected");
  selectedButton?.setAttribute("aria-pressed", "false");
  els.definitionCard.classList.add("is-wrong");
  state.selectedCandidate = null;
  els.matchButton.disabled = true;
  els.feedbackPanel.hidden = false;
  els.feedbackPanel.classList.add("is-wrong");
  els.feedbackTitle.textContent = "Bu çift olmadı";
  els.feedbackText.textContent = "Bu cevap başka bir tanıma ait. Başka bir cevap kartı seçip yeniden dene.";
}

function completeDefinition() {
  const question = state.session[state.index];
  state.completedQuestionIds.add(question.id);
  state.solved += 1;
  updateScore();

  if (state.index >= state.session.length - 1) {
    showResults();
    return;
  }

  state.index += 1;
  renderQuestion();
}

function judgeCandidate() {
  const question = state.session[state.index];
  const candidate = state.selectedCandidate;
  if (!candidate) {
    return;
  }

  const actualMatch = normalizeAnswer(candidate) === normalizeAnswer(question.answer);

  if (!actualMatch) {
    showDecisionError(question);
    return;
  }

  completeDefinition();
}

function showResults() {
  const total = state.session.length;

  els.quizPanel.hidden = true;
  els.resultPanel.hidden = false;
  els.resultTitle.textContent = `${total} kart çiftini buldun`;

  if (state.errors === 0) {
    els.resultText.textContent = "Tüm kartları hiç hata yapmadan eşleştirdin.";
  } else if (state.mistakes.length === 1) {
    els.resultText.textContent = `${state.errors} kez yanlış kartı denedin. Zorlandığın kartı tekrar görebilirsin.`;
  } else {
    els.resultText.textContent = `${state.errors} kez yanlış kartı denedin. ${state.mistakes.length} kart çiftini tekrar edebilirsin.`;
  }

  els.retryMistakesButton.hidden = state.mistakes.length === 0;
}

function showSetup() {
  els.quizPanel.hidden = true;
  els.resultPanel.hidden = true;
  els.setupPanel.hidden = false;
  els.startButton.focus({ preventScroll: true });
}

function retryMistakes() {
  const questions = [...state.mistakes];
  if (questions.length > 0) {
    startSession(questions);
  }
}

function handleKeyboard(event) {
  if (!els.quizPanel.hidden && event.key === "ArrowRight") {
    event.preventDefault();
    judgeCandidate();
  }
}

els.startButton.addEventListener("click", () => startSession());
els.restartButton.addEventListener("click", showSetup);
els.matchButton.addEventListener("click", judgeCandidate);
els.retryMistakesButton.addEventListener("click", retryMistakes);
els.newSessionButton.addEventListener("click", showSetup);
document.addEventListener("keydown", handleKeyboard);

renderTopicOptions();
updateScore();
